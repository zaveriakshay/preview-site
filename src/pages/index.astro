---
// This page will automatically redirect users from the root URL (`/`)
// to the default language's homepage, ensuring they always land on a
// page with the correct Starlight context.

// For this project, the default language is 'en'.
return Astro.redirect('/en/');
---